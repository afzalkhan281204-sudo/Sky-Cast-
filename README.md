# Sky-Cast-
SkyCast-ML is a time-series weather forecasting system built on a Long Short-Term Memory (LSTM) neural network implemented entirely from scratch using only NumPy. It takes a sequence of recent weather observations and predicts the next 7 days of maximum temperatures — with uncertainty estimates for each day.
